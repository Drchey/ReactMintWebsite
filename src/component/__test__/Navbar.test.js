// import { screen, render } from '@testing-library/react'
// import Navbar from '../index'

/**
 * Screens
 * 1) Get By, Find By and Query By Just gets one element if
 * they match more than one element you will get an Error
 * 2) GetAllBy, FindAllBy, QueryAllBy Returns an Array of element that match the condition
 * 3) queryBy ll return null if there is no match => Great for when we dont or not expect
 * 4) findBy & FindAllBy are used for async await calls
 *
 * e.g it("Find", async () => {
 *     const findElement = await screen.findByText("")
 * })
 */

/**
 *  ByRole - button, header,
 * By Label - Label Text
 *
 */

/**
 *  Utilizing the with react-router-dom
 *
 *
 * Assertions
 * toBeInTheDocument
 *
 */

// This is a Test  Test Block We Can Use test() or it()
// test('renders learn react link', () => {
//   render(<Navbar title="" />) // We Render the Component That We Wish to Test Here
//    const linkElement = screen. // Find Elements We Want to Interact With
//   const headingElement = screen.getByText
// }) // Description

// it('', () => {

// })
